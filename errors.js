/**
* Collect errors that should be displayed nicely here.
*/

const errors = {
  'canvas.getContext is not a function': 'Was the function called before the canvas was mounted?',
};

module.exports = errors;
